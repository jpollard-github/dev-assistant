# Runtime mode. Keep local-only unless you explicitly opt into hosted providers.
DEV_ASSISTANT_MODE=local-only

# Local model defaults.
DEV_ASSISTANT_MODEL_PROVIDER=ollama
DEV_ASSISTANT_MODEL_NAME=qwen2.5-coder:7b

# Optional override for assistant state. Defaults to <repo>/.dev-assistant.
# DEV_ASSISTANT_DATA_DIR=.dev-assistant
